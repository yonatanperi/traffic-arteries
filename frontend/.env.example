# Base URL of the Django backend (scheme + host, no trailing slash, no /api).
# Leave empty for local dev — the Vite dev server proxies /api to :8000.
# In production (Cloudflare Pages) set this to the Render backend origin.
VITE_API_URL=https://traffic-arteries.onrender.com
